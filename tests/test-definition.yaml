---
dimensions:
  - name: openshift
    values:
      - "false"
tests:
  - name: restarter
    dimensions:
      - openshift
  - name: restarter-no-unneeded-restart
    dimensions:
      - openshift
  - name: restarter-content-identical-write
    dimensions:
      - openshift
suites:
  - name: nightly
  - name: openshift
    patch:
      - dimensions:
          - name: openshift
            expr: "true"
